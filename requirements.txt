# CPU-only torch: the CUDA wheel is ~2.5 GB and a free Space has no GPU.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.13.0
numpy==2.5.2
scipy==1.16.2
rasterio==1.5.1
scikit-image==0.26.0
pandas==3.0.5
pillow==12.3.0
safetensors==0.8.0
streamlit==1.62.0

# SEN2SR fidelity branch (ESA OpenSR) and its model loader.
sen2sr==0.8.5
mlstac==0.4.9
